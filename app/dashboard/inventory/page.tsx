import { classNames } from "@/libs";
import React from "react";

const Products = () => {
  return (
    <div className={classNames("flex flex-col", "shadow-sm", "w-full")}>
      Products
    </div>
  );
};

export default Products;
